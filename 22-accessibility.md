# Accessibility (A11y)

Use semantic markup, manage focus, ensure keyboard navigation, and ARIA roles where needed.

```html
<button aria-pressed="false">Toggle</button>
```
**Expected output:** Screen readers announce state; tab order is logical.
