# Render Functions & JSX

JSX can be used with Vite plugin config; prefer templates for most cases.

```ts
// vite.config.ts add @vitejs/plugin-vue-jsx
```
**Expected output:** Components can be written in TSX with full typing.
