# SSR/SSG with Nuxt 3 (Optional)

Nuxt adds pages/layouts, server routes via Nitro, and SSR/SSG pipelines.

```bash
npx nuxi init my-nuxt-app
```
**Expected output:** Nuxt starter with SSR/SSG ready; use `useAsyncData`/`useFetch` for data.
