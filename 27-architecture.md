# Modules & Architecture

Adopt feature folders, shared UI libs, and clear dependency boundaries.

```txt
src/
  features/
  components/
  composables/
  stores/
  pages/
```
**Expected output:** Large apps remain maintainable; teams can work in parallel.
