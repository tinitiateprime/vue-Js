# GraphQL with Vue

Apollo Client integrates with Vue for queries/mutations/subscriptions.

```bash
npm i @apollo/client graphql
```
**Expected output:** Write GQL queries; cache normalizes entities for fewer requests.
