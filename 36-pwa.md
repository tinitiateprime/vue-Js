# PWA & Offline

Add a service worker, offline caching, push notifications, and a web app manifest.

```json
// public/manifest.webmanifest
{ "name": "My Vue App", "start_url": "/", "display": "standalone" }
```
**Expected output:** App installable with basic offline capability.
