# Testing Suite

Unit test with Vitest + Vue Test Utils; E2E with Cypress/Playwright.

```bash
npm i -D vitest @vue/test-utils jsdom
```
**Expected output:** Run `vitest` to execute component tests; mock router/pinia as needed.
