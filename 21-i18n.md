# Internationalization (i18n)

Use `vue-i18n` for messages, plurals, and lazy-loaded locales.

```bash
npm i vue-i18n
```
**Expected output:** Localized strings with formatting for numbers/dates; switch language at runtime.
